# Use this config when you run playbooks from this directory.
# (Ansible only reads ./ansible.cfg from the current working directory, not from parent folders.)
[defaults]
inventory = ../inventory.sh
remote_user = ubuntu
host_key_checking = False
timeout = 30

[inventory]
enable_plugins = script, ini, auto